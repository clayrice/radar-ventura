import Link from 'next/link';import {redirect} from 'next/navigation';import {isDemo,configured,sessionDb} from '@/lib/db';import {signOut} from '@/app/actions';
export const dynamic='force-dynamic';
export default async function AccountLayout({children}:{children:React.ReactNode}){const demo=isDemo();if(!demo){if(!configured())redirect('/login');const db=await sessionDb();const {data:{user}}=await db.auth.getUser();if(!user)redirect('/login');}return <main id="main" className="wrap account-grid"><aside className="account-sidebar"><span className="eyebrow">SUA EDIÇÃO SEMANAL</span><h2>Minha Ventura</h2><Link href="/account">Minhas edições ↗</Link><Link href="/account/profile">Perfil da empresa ↗</Link><Link href="/account/rota">Meu Plano de Rota ↗</Link><Link href="/account/parceiro">Meu perfil de parceiro ↗</Link><Link href="/partners">Encontrar um parceiro ↗</Link>{demo?<p className="notice">Conta de exemplo. Nenhum dado pessoal é salvo e nenhum email é enviado.</p>:<form action={signOut}><button className="button outline">Sair</button></form>}</aside><div>{children}</div></main>}

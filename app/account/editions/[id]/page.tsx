import {notFound} from 'next/navigation';import {isDemo,sessionDb,checked} from '@/lib/db';import {demoEdition} from '@/lib/demo';import {EditionView} from '@/components/edition-view';import type {Edition} from '@/lib/types';
export default async function EditionPage({params}:{params:Promise<{id:string}>}){const {id}=await params;if(isDemo()){if(id!==demoEdition.id)notFound();return <EditionView edition={demoEdition}/>;}if(!/^[0-9a-f-]{36}$/.test(id))notFound();const db=await sessionDb();const e=checked(await db.from('editions').select('id,week_start,status,sent_at,content,articles,partners').eq('id',id).maybeSingle()) as Edition|null;if(!e)notFound();return <EditionView edition={e}/>;}

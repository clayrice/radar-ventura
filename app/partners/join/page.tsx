import Link from 'next/link';import {partnerPlans,priceLabel} from '@/lib/products';
export default function Join(){return <main id="main" className="wrap"><section className="page-heading"><span className="eyebrow">CRESÇA COM A VENTURA</span><h1>Sua especialidade.<br/><em>O próximo passo de uma empresa.</em></h1><p>Apresente o que você resolve e encontre empresas que já estão pensando em aplicar IA na operação. O cadastro é feito online, pela sua conta.</p></section><div className="plan-grid pricing">{partnerPlans.map(p=><article className="partner-card" key={p.id}><span className="tag">ASSINATURA MENSAL</span><h2>{p.name}</h2><p>{p.description}</p><ul>{p.features.map(f=><li key={f}>{f}</li>)}</ul><p className="price">{priceLabel(p.id)}<small>/mês</small></p><Link className="button" href="/login?next=/account/parceiro">Cadastrar meu perfil ↗</Link></article>)}</div><p className="notice">A presença em recomendações depende da compatibilidade com o negócio e o projeto. A assinatura não garante quantidade de indicações ou contatos. Os conteúdos patrocinados serão identificados.</p></main>}

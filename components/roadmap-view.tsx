import Link from 'next/link';import type {RoadmapOutput} from '@/lib/roadmap-schema';import type {Partner} from '@/lib/types';
export function RoadmapView({output,partners,demo=false}:{output:RoadmapOutput;partners:Partner[];demo?:boolean}){return <><div className="edition-head"><span className="eyebrow">PLANO DE ROTA IA{demo?' · EXEMPLO':''}</span><h1>Três caminhos para <em>começar.</em></h1></div>{demo&&<p className="notice">Resultado demonstrativo para uma imobiliária. Estas rotas não foram geradas a partir das suas respostas.</p>}<p>{output.diagnosis}</p>{output.projects.map((project,i)=><section className="edition-section" key={i}><span className="eyebrow">ROTA 0{i+1} · ESFORÇO {project.effort.toUpperCase()}</span><h2>{project.title}</h2><p className="muted">{project.problem}</p><div className="panel"><h3>Projeto sugerido</h3><p>{project.solution}</p><h3 style={{marginTop:24}}>Primeiro passo</h3><p>{project.first_step}</p><h3 style={{marginTop:24}}>Como avaliar</h3><p>{project.success_metric}</p><ol>{project.phases.map(phase=><li key={phase}>{phase}</li>)}</ol></div><span className="mini-label">PARCEIROS PARA ESTA ROTA</span>{partners.filter(p=>p.capabilities.includes(project.capability)).length?partners.filter(p=>p.capabilities.includes(project.capability)).slice(0,2).map(p=><article className="partner-placement" key={p.id}><span className="sponsored">Parceiro patrocinado{demo?' · Exemplo':''}</span><h3>{p.name}</h3><p>{p.description}</p><Link className="text-link" href={`/partners/${p.slug}`}>Conhecer o parceiro ↗</Link></article>):<p className="notice">Ainda não há parceiro compatível com esta rota.</p>}</section>)}</>}
